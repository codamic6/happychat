
'use server';

/**
 * MEGA integration has been removed.
 * File kept for reference or future non-storage profile actions.
 */

export async function placeholderAction() {
  return { success: true };
}
